name: Exploring variables and secrets

on: push

env:
    CONTAINER_REGISTRY: docker.io
    DOCKER_USERNAME: diallodev221
    DOCKER_PASSWORD: S@ftL3tm3In
    IMAGE_NAME: github-actions-nginx

jobs:
    docker:
        runs-on: ubuntu-latest
        steps:
          - name: Docker build
            run: echo docker build -t $CONTAINER_REGISTER/$DOCKER_USERNAME/$IMAGE_NAME:latest

          - name: Docker login
            run: echo docker login --username $DOCKER_USERNAME --password $DOCKER_PASSWORD

          - name: Docker publish
            run: echo docker push $DOCKER_USERNAME/$IMAGE_NAME:latest
    
    deploy:
        needs: docker
        runs-on: ubuntu-latest
        steps:
          - name: Docker run
            run: echo docker run -d -p 8080:80 $DOCKER_USERNAME/$IMAGE_NAME:latest
            